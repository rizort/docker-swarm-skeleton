services:
  php:
    environment:
      - APP_ENV=prod
      - APP_SECRET_FILE=/run/secrets/app_secret

  nginx:
    environment:
      - APP_ENV=prod

secrets:
  app_secret:
    external: true

networks:
  webnet:
    driver: overlay